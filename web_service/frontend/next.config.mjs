/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  transpilePackages: ["@wanteddev/wds", "@wanteddev/wds-icon", "@wanteddev/wds-nextjs"],
};

export default nextConfig;
